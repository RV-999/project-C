/* all global variables are declated in main.c file and start with g_ to distinguish them.
They are explained in main.c here they are defined so we can use them in other files */
extern int IC ;
extern int DC ;
extern int ICF;
extern int DCF;
extern symbol_struct* symbol_table;
extern int nums ;
extern mem* wd;
extern int numd ;
extern int numc;
extern int numen;
extern ext* extPrint;
extern word *code_tabel;
extern int numext;
extern int IC2;
extern actions actionshash[16];
